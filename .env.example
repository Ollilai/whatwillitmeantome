
/**
 * @description
 * This file provides an example set of environment variables needed by the application.
 * It includes placeholders for DB, Supabase, Clerk, and AI credentials.
 *
 * Key changes in this step:
 * - Added `MISTRAL_API_KEY=` to hold Mistral's API key.
 *
 * @notes
 * - Never commit actual secrets to version control. Put real values in `.env.local`.
 * - The `.gitignore` already excludes `.env.local`, so we do not expose secrets in the repo.
 */

BEGIN WRITING FILE CODE

# DBpostgresql://postgres.eibnapzugfwvxdxhzins:Kikulikukka123-@aws-0-eu-north-1.pooler.supabase.com:6543/postgres

# Supabase
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_BUCKET_RECEIPTS=

# Auth
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/login
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/signup

# AI
HF_API_KEY=
MISTRAL_API_KEY=